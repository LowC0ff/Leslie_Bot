// renomear para esse arquivo para .env

const token = ''

module.exports = {
    token,
    apiUrl: `https://api.telegram.org/bot${token}`,
    aptFileUrl: `https://api.telegram.org/file/bot${token}`
}